FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (like dev does)
RUN npm ci

# Copy all files
COPY . .

# Expose port
EXPOSE 3000

# Just run the dev server like in development (avoids all build issues)
CMD ["sh", "-c", "npx react-router dev --host 0.0.0.0 --port 3000"]