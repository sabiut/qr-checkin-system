FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Copy all files
COPY . .

# Expose port
EXPOSE 3000

# Run exactly like dev setup - uses main vite.config.ts with allowedHosts
CMD ["sh", "-c", "npm ci && npx vite --host 0.0.0.0 --port 3000"]