FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Copy all files
COPY . .

# Expose port
EXPOSE 3000

# Run using npm dev script which properly configures React Router
CMD ["sh", "-c", "NODE_ENV=development npm ci && npm run dev -- --host 0.0.0.0 --port 3000"]